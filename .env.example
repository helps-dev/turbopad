# TurboPad deployer credentials — copy this file to ".env" and fill in your key.
# NEVER commit the real .env. NEVER paste the key anywhere else (chat included).
# Use a DEDICATED deployer wallet holding only the ETH you need for gas.

# Private key of the deployer wallet, with 0x prefix:
TURBOPAD_DEPLOYER_KEY=0xGANTI_DENGAN_PRIVATE_KEY_ANDA
